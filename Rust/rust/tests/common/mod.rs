/** We can use the common folder to specify functions that multiple test files use and not test this function. We can call this file
 *  by using `mod common;` in our test files.
 */
pub fn setup() {
    // -snip-
}